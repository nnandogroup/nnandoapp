import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1068ee",
    },
    secondary: {
      main: "#d1d9e7",
    },
  },
  typography: {
    fontFamily: "Manrope",
  },
});
